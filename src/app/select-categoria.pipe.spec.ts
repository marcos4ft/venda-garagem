import { SelectCategoriaPipe } from './select-categoria.pipe';

describe('SelectCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
