import { SelectEstadoPipe } from './select-estado.pipe';

describe('SelectEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
